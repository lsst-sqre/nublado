# -*- conf -*-
#
# Editable runtime dependencies for the inithome provisioner. Add direct
# runtime dependencies here, as well as implicit dependencies with constrained
# versions.
#
# After editing, update inithome/requirements/main.txt by running:
#     nox -s update-deps

# inithome currently only requires modules that ship with Python.

<!-- Delete the sections that don't apply -->

### Backwards-incompatible changes

- Replace use of `RSP_SITE_TYPE` with explicit configuration.

### New features

- Add `config.json` ConfigMap to replace environment for UI configuration.

- Add `enable_landing_page` configuration item.

- Add `enable_query_menu` configuration item.

- Add `enable_tutorials_menu` configuration item.

- Add `file_browser_root` setting to determine top of Lab file browser.

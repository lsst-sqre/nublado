<!-- Delete the sections that don't apply -->

### Other changes

- Install typst from pypi rather than the raw binary
- Remove pandoc (Callisto can handle CST-style embedded images now)
- Remove jupyterlab-variable-inspector (it no longer works with JL 4 and causes kernel instability)

<!-- Delete the sections that don't apply -->

### Backwards-incompatible changes

- Users of the standard home directory provisioner must specify `nublado inithome` as the container command.

### New Features

- Filesystem administrators now have the Nublado CLI available (e.g., the user home directory provisioner) in the default administration container.

<!-- Delete the sections that don't apply -->

### Backwards-incompatible changes

- Consolidate everything but hub and client under `nublado` namespace.

- Consolidate package containers into `nublado` container.

- Allow specification of container commands for `fsadmin` and init containers.


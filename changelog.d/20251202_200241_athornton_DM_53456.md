<!-- Delete the sections that don't apply -->

### Backwards-incompatible changes

- Users of `inithome` must specify `nublado inithome` as the container
command.

### New Features

- Nublado CLI is available to users of `fsadmin`.
